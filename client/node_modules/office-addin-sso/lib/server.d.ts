export declare class SSOService {
    private app;
    manifestPath: string;
    private port;
    private server;
    private ssoServiceStarted;
    constructor(manifestPath: string);
    startSsoService(isTest?: boolean): Promise<boolean>;
    private getSecret;
    getTestServerState(): boolean;
    startServer(app: any, port: any): Promise<boolean>;
    stopServer(): Promise<boolean>;
}
